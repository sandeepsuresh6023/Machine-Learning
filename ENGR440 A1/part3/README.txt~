The file perceptron.py is the perceptron network> iit runs for 1000 epochs.
The folder Executable files contains the executable files for the progran.
The training and testing files used are also here.
